# Rejected Ideas

Material here has been intentionally rejected.

Agents must not reuse it as default inspiration. A rejected idea may only return when the author explicitly asks to reconsider it.

For important rejected concepts, record:

```yaml
status: rejected
do-not-resurrect: true
reason: "..."
```
